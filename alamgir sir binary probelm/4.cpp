#include <bits/stdc++.h>
using namespace std;
void solve(int arr[], int n)
{
    int l = 0, r = n - 1;
    int res = 0;
    while (l <= r)
    {
        int mid = (l + r) / 2;
        if (arr[mid] ==)
    }
}
int main()
{
    int n;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
        cin >> arr[i];

    sort(arr, arr + n);
    solve(arr, n);
}
// bakita pore korbo